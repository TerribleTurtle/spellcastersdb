# API Configuration
# The base URL for the Spellcasters Community API.
# Defaults to the live GitHub Pages API if not set.
# For local API development, point this to your local server (e.g., http://localhost:8000/api/v2)
NEXT_PUBLIC_API_URL=https://terribleturtle.github.io/spellcasters-community-api/api/v2

# Asset Format Preference
# Controls the preferred image format for fetching assets.
# Options: "webp" (default, recommended), "png" (legacy/fallback)
NEXT_PUBLIC_PREFERRED_ASSET_FORMAT=webp

# Revalidation Secret
# Used to secure the /api/revalidate endpoint from unauthorized access.
# Required for daily cache revalidation via GitHub Actions.
# REVALIDATION_SECRET=

# GitHub API Token (Optional)
# Used to increase rate limits for fetching roadmap and issue data.
# GITHUB_TOKEN=

# -----------------------------------------------------------------------------
# Local Development Toggles
# -----------------------------------------------------------------------------

# Toggle to serve images from the local public/ folder instead of remote URLs.
# Useful when working offline or testing strict asset loading.
NEXT_PUBLIC_USE_LOCAL_ASSETS=false

# -----------------------------------------------------------------------------
# Local API Override (Optional)
# -----------------------------------------------------------------------------

# If set, the app will strictly load data from this JSON file in 'development' mode.
# If unset, it attempts to find the API repo as a sibling folder automatically.
#
# Mac/Linux Example:
# LOCAL_DATA_PATH=/Users/username/Projects/spellcasters-community-api/api/v2/all_data.json
#
# Windows Example:
# LOCAL_DATA_PATH=C:\Projects\spellcasters-community-api\api\v2\all_data.json

# Legacy Fallback (Deprecated but supported)
# LOCAL_API_PATH=C:\Projects\spellcasters-community-api\api\v2\all_data.json

# -----------------------------------------------------------------------------
# Dev Server Port (Optional)
# -----------------------------------------------------------------------------
# Used by the local patch-history proxy. Defaults to 3000 if not set.
# PORT=3000

# -----------------------------------------------------------------------------
# Rate Limiting (Upstash Redis) - Optional
# -----------------------------------------------------------------------------
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=

# -----------------------------------------------------------------------------
# Development & Testing
# -----------------------------------------------------------------------------
# LOCAL_ASSETS_PATH="C:\path\to\local\assets"

