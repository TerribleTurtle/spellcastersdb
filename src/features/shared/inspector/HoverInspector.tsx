// HoverInspector is currently disabled/unused in favor of persistent InspectorPanel
// Keeping file as placeholder if needed later, or could be deleted.

export function HoverInspector() {
  // Disabled as we now use the persistent InspectorPanel
  return null;
}
